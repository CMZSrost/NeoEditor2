<UserControl
    x:Class="NeoEditor.Views.Controls.EditXmlPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:NeoEditor.ViewModels.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Name="editXml"
    d:DataContext="{d:DesignInstance Type=controls:EditXmlViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox
            Grid.Row="0"
            Margin="5"
            VerticalAlignment="Center"
            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="Foreground" Value="Gray" />
                    <Style.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Foreground" Value="Black" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>

        <DataGrid
            Grid.Row="1"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ItemsSource="{Binding FilteredNodes}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="节点名称" />
                <DataGridTextColumn Binding="{Binding Value}" Header="值" />
                <DataGridTextColumn Binding="{Binding Attributes}" Header="属性" />
                <DataGridTextColumn Binding="{Binding Level}" Header="层级" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>