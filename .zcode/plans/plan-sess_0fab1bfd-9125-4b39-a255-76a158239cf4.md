# Round 30：字段解释 + 可视化 + 引用解析跳转

## 现状诊断
- **字段解释**：DataTable 列头有 `FieldDescriptionService`（docx 提取），但提取 key 是 `attackmodes攻击模式.strname` 而查询 key 是 `attackmodes.strname` → **实际不生效**；Value Editor 完全无解释；权威数据在 Doc 38（24 表全字段含义+实测值域，与 C# 属性名基本一致）
- **可视化**：24 个专属 visualizer + Default 已有；Doc 21 §7 的 P6（徽章悬停预览）未实现
- **引用跳转**：DataTable Ctrl+Click/Ctrl+右键/Ctrl+Hover 已统一（round29）；但 **Value Editor 徽章无任何跳转**；导出路径（CSV/MCP/CLI）仍输出损坏 `[a, b]`（round29 已知限制）

用户已确认：跳转语义与 DataTable 一致（Ctrl+Click）；可视化做 P6 + 检查清单补齐；导出清扫一并做。

## A. 字段解释（所有字段）
1. **生成权威数据源**：脚本 `artifacts/gen-field-descriptions.js` 解析 `Docs/38-full-field-reference.md` 24 张表的 markdown 表格 → `{table}.{property}` → 「含义 + 实测值域」文本（引用列保留 `🔗 引用列 → X`）。属性名不一致处用别名表（如 ItemType.CondId→ConditionId，实施时由覆盖测试发现全部差异）
2. **嵌入 Core**：产物 `field_descriptions.json` 作 EmbeddedResource 嵌入 `NeoEditor.Core`；新增 `NeoEditor.Core/Model/FieldDescriptions.cs` 静态只读类（仿 `FieldGroupMetadata` 模式，`GetDescription(Type, propName)`，符合 N01）
3. **接线 3 条显示路径**：
   - DataTable 列头 ToolTip：`App.axaml.cs` 的 `FieldDescriptionProvider` 改「内嵌资源优先 → docx 缓存兜底」（修复 key 不匹配）
   - Value Editor：`FieldRow.Description` 赋值（引用字段追加 Pattern/分隔符说明）+ `KeyValueEditorView.axaml` 行 DisplayName 挂 `ToolTip.Tip`
   - Visualizer `BuildRawDataTable`：每行加字段解释 ToolTip
4. **覆盖测试**：24 实体类型全部 `[Column]` 属性都有描述（缺即失败 → 驱动别名表补齐）

## B. 可视化
1. **P6 徽章悬停预览**：`VisHelperService.BuildRefTooltip(IEntity)`（按类型分派小型 stat 面板，default=Subject+Id）；`RefNode` 构造加可选 `Func<IEntity, Control?>? tooltipBuilder`，`Badge<T>`/`BadgeForEntity`/`BadgeWithSlot` 解析成功后挂 ToolTip；Value Editor 徽章同样挂预览
2. **检查清单补齐（Doc 21 §10）**：审计 24 个 visualizer（Hero Header/Type Badge/Flag/StatBar/引用徽章+跳转/反向引用/长文本/RawData），补缺面板；`BuildRawDataTable` 引用值显示解析后 Subject（目前只显示原文）

## C. 引用字段解析跳转
1. **Value Editor 徽章跳转（与 DataTable 一致）**：`ReferenceFieldEditor.CreateBadge` 加 `WireNavigation` 语义 —— Ctrl+Click → `FindBestMatch` → `INavigationRouter.NavigateToEntity`；Ctrl+右键 → `RequestPeek`（round29 统一语义不变）
2. **导出清扫**：`CsvImportExportService`(:82/:142/:143/:163)、`EntityResourceProvider`(:66)、`CliCommandHandler`(:204/:286) 的 `prop.GetValue(entity)?.ToString()` → `ReferenceText.GetRawString(value, refAttr)`

## D. 测试
- Core.Tests：`FieldDescriptionsTests`（资源加载 + 全 `[Column]` 属性覆盖断言）
- Infra.Tests：CSV 导出引用列输出原文
- Mcp.Tests / Cli.Tests：引用列输出原文
- EntityEditor.Tests：`FieldRow.Description` 赋值 + 徽章 Ctrl+Click 发导航消息（headless TestApp 已就绪）
- 全量 `dotnet build` + 全量测试通过

## E. 文档
- 新增 `test_round30_summary.md`，更新 `CLAUDE.md`

## 风险与注意
- markdown 表格解析需处理单元格内 `|`（示例值列），含义列一般无；实施时验证
- `Core.csproj` 加 `<EmbeddedResource Include="Model/field_descriptions.json" />`
- 模型属性名与 Doc 38 不一致处靠覆盖测试兜底 + 别名表