# Spec 验收审计报告 — 2026-07-05

> 对照 16 条基石规则 (R01-R16) + 6 条禁止规则 (N01-N06) + 4 个已决问题 (Q7-Q10) 逐条审计。
>
> **补记 2026-07-17**：R09 脏提示细化 + Mod XML 文件列表修复，详见文末。
>
> **补记 2026-07-18**：T1-T7 全部落地、审计状态大幅修正；T8 `_copyBuffer` 去静态化，详见文末。
>
> **补记 2026-07-24**：Bug 1 KV 焦点跟随修复 + GDH 导航提取 + App.ServiceProvider 进一步收紧，详见文末。

## 总览

| 状态 | 数量 | 占比 |
|------|:----:|:----:|
| ✅ 完全落地 | 20 | 77% |
| ⚠️ 部分落地 | 2 | 8% |
| ❌ 未完成 | 0 | 0% |
| 待验证 | 1 | 4% |
| 架构债 | 2 | 8% |

---

## ✅ 完全落地（13 项）

### 基石规则

| 规则 | 标题 | 证据 |
|------|------|------|
| R01 | 状态唯一所有者 IWorkspaceSession | `WorkspaceSession` 类已实现，GDH 所有 store 属性已委托到 Session |
| R02 | 单活跃 Session | `WorkspaceSession` 已 DI 注册为 Scoped |
| R06 | 四区域数据同源 | `IWorkspaceSession.Store` 统一入口：`ActiveMergeStore ?? BrowserStore` |
| R08 | 编辑入口仅 KV 与 XML | Bottom DataTable 只读 + 跳转，配合 R15 交互矩阵 |
| R09 | 脏数据视觉指示 | `DirtyEntities` 完整链路；Sidebar 标题下方脏计数 + HomePage 内联 `⚠ unsaved` / `⚠ {n} dirty` 徽章 |
| R11 | 文档独立保存 | `EntityEditorDocument` 独立 `MarkDirty`/`ClearDirty`；工具栏「Save Session」全局保存 |
| R12 | 选中由 ISelectionService 统一管理 | `SelectionService` 已实现，接口含 `CurrentEntity`/`SetCurrentEntity`/`RequestOpenEntity`/`RequestNavigate` |
| R13 | VisHelper 为 internal static | 25 个 `*EntityVisualizer.cs` 文件 + 单一 `VisHelper` 类 |
| R14 | 文件夹约定分层 | 单程序集 + 命名空间约定 |
| R15 | DataTable 交互矩阵 | `SuppressNextSelectionChanged` 机制完整实现，Tunnel 阶段优先处理 |

| R03 | 引用解析只走注入的 IReferenceResolver | `ReferenceResolver` 全量实例化：`_lookupCache`/`_cachedIndexService` 为实例字段，`BuildReverseIndexAsync`/`ResolveReverseRefs` 为实例方法，0 处 `static` 修饰符 |

### 禁止规则

| 规则 | 标题 | 证据 |
|------|------|------|
| N02 | 禁止 ReferenceResolver.Instance | 全代码库 0 匹配——已彻底删除 |
| N04 | 禁止死消息 | `GridRowHeightChangedMessage`/`FontSizeChangedMessage`/`SwitchToSettingsMessage` 已删除——全代码库 0 处匹配 |
| N05 | 禁止 Bottom DataTable 原地编辑 | Bottom 只读 + 跳转，无单元格编辑能力 |

### 已决问题

| 问题 | 标题 | 证据 |
|------|------|------|
| Q7 | Dialog code-behind 服务获取 | `CreateModDialog`/`RenameImagePairDialog`/`AddRowDialog` 均有 `Create(IServiceProvider)` 静态工厂 + `ShowAsync`/`ShowSimpleAsync` 接收注入的 `IConfigService` |
| Q9 | ImagePreviewContent._cachedImgDirs | 已迁移到 `ImageService` 实例属性 `_cachedImgDirs` |
| Q10 | 死消息处理策略（方案A：全部删除） | 与 N04 共同落地——3 条死消息全部删除 |

---

## ⚠️ 部分落地（4 项）

### R04 — View 只组装控件

**已做到**：`INavigationRouter` 已注入 `VisHelper`，25 个 Visualizer 各拆一文件。

**残余问题**：`GenericDataGridHelper`（Helper 层，非 View 层）包含大量导航逻辑：
- 静态方法 `RaiseCellEditCommitted` / `RaiseCloneRowRequested` / `RaiseFindReferencesRequested` 通过 `WeakReferenceMessenger.Default` 直接发送消息
- `ConfigureColumn` 内联了大量 Ctrl+LMB/Ctrl+RMB 事件处理逻辑（600+ 行）
- 引用单元格格式化方法 `FormatSegmentDisplay` 包含 `LookupSubjectByRawId` 调用

**文件**：`NeoEditor/Helper/GenericDataGridHelper.cs`

---

### R07 — 单向分层

**已做到**：文件夹布局 Domain / Services / ViewModels / Views 基本符合。

**残余问题**：`GenericDataGridHelper`（Helper 层）通过 `App.NavigationRouter`、`App.WorkspaceSession`、
`App.ReferenceResolver` 等 V6 访问器获取 DI 服务，违反单向依赖原则（按 R07 应仅 View 层可反向依赖 App）。

---

### R10 — 索引手动刷新，编辑后标过期不自动重建

**已做到**：`ForwardIndex`/`ReverseIndex` 在 `IWorkspaceSession` 中定义。

**待验证**：UI 上是否有过期角标展示，以及手动刷新按钮。需要人工验证运行时的交互行为。

---

### N01 — 禁止静态可变状态

**已做到**：
- `ActiveMergeStore` / `BrowserStore.Instance` 已消除，改为委托到 `IWorkspaceSession`。
- T7（2026-07-18）将 GDH 残余 4 个静态可变字段提取到 `DataGridInteractionState` 注入单例：
  `ColumnMetaCache`、`_ctrlWasPressed`、`NavigationHandled`（死代码，已删除）、`SuppressNextSelectionChanged`
  全部通过 `App.DataGridState` 委托。
- `_router`/`_session` 已改为 `App.NavigationRouter`/`App.WorkspaceSession` 的延迟属性（合规的
  V6 模式，不持有状态）。
- `FieldDescriptions` 为启动时赋值一次、此后只读的 immutable reference。
- `_copyBuffer`（T8, 2026-07-18）从 `private static string?` 改为 `private string?` 实例字段——
  View code-behind 中最后一个残余静态可变字段，T7 未覆盖因其不在 GDH 中。

**文件**：`NeoEditor/Services/DataGridInteractionState.cs` — 新建的注入单例服务
`NeoEditor/Views/UserControls/ModGameDataTabsView.axaml.cs` — `_copyBuffer` 去静态化

---

### N03 — 禁止 View 写业务/导航逻辑

**已做到**：Visualizer 通过 `VisHelper.NavLeaf` 声明式创建可导航控件。
`ModGameDataTabsView` code-behind 中对 `DirtyEntities` 的所有操作已通过 `IWorkspaceSession`
封装方法完成：`MarkEntityDirty()` / `MarkEntitiesDirty()` / `ClearDirtyEntities()` /
`RemoveDirtyEntities()`。

**残余问题**：View 层仍包含 WAL 恢复逻辑（`MarkTabsDirtyFromEditedCells` L540-563）和
`_commandHistory` 状态管理，这些应下沉到 ViewModel 或专用 Service。

---

## 🔴 架构债（跨规则的深层次问题）

### 1. GenericDataGridHelper 仍是巨型静态类（降级）

T7 已将 4 个静态可变字段提取到 `DataGridInteractionState` 注入单例，消除了 N01 违规。
但 GDH 本身仍为 `static class`，包含 ~600 行 `ConfigureColumn` 导航逻辑（含事件处理器内联代码），
通过 `App.*` V6 访问器反向抓取 DI 服务。

**违反规则**：R04（Helper 层含导航逻辑）、R07（Helper 层反向依赖 App.*）

### 2. App.ServiceProvider 引用大幅缩减

以下位置仍有 `App.ServiceProvider!.GetRequiredService<T>()` 调用（2026-07-18 T6 后）：

| 文件 | 行号 | 获取的服务 | 说明 |
|------|------|-----------|------|
| CreateModDialog.axaml.cs | 15 | CreateModDialogViewModel | Avalonia 参数化构造器回退，Q7=C 工厂已在所有运行时调用点使用 |
| RenameImagePairDialog.axaml.cs | 15 | RenameImagePairDialogViewModel | 同上 |
| HomePage.axaml.cs | 67 | MainWindowViewModel | Scoped 服务，无法用 `App.*` 静态持有 |

> **T6 后对比**：`App.ServiceProvider.GetRequiredService<T>()` 在 View 层 code-behind
> 从 **~22 处** 降至 **3 处**（+1 处 `App.ServiceProvider!` 仅作工厂参数传递）。
> 新增 7 个 `App.*` V6 访问器：`GameDbFactory`, `EditorDbFactory`, `LoggerFactory`,
> `XmlParser`, `WorkspacePersistence`, `ProfileManager`, `MergeService`。

**违反规则**：N01 规定「仅 composition root 可用」

### 3. ModGameDataTabsView code-behind 部分违反 N03

View 层 `DirtyEntities` 直接操作已修正（改用 Session 封装方法），但 WAL 恢复逻辑
和 `_commandHistory` 状态管理仍留在 View code-behind 中。

**违反规则**：N03

---

## 📋 建议执行顺序

### ✅ 已完成（7 项）

| # | 任务 | 对应规则 | 证据 |
|---|------|---------|------|
| 1 | 删除 `GridRowHeightChangedMessage` 及其发送方 | N04, Q10 | `grep` — 全代码库 0 匹配 |
| 2 | `AddRowDialog` 添加 `Create(IServiceProvider)` 工厂方法 | Q7 | `AddRowDialog.axaml.cs:134` — `Create(IConfigService, ...)` 已实现 |
| 3 | 迁移 `ReferenceResolver` 静态成员为实例成员 | R03, N01 | `ReferenceResolver.cs` — 0 处 `static` 修饰符 |
| 4 | 清理 `GenericDataGridHelper` 中 `App.ServiceProvider` 引用 | R07, N01 | GDH 0 处 `App.ServiceProvider`；已改用 V6 `App.*` 静态访问器 |
| 5 | 移除 `ModGameDataTabsView` code-behind 中的 `DirtyEntities` 直接操作 | N03 | 全部改用 `WorkspaceSession.MarkEntityDirty`/`MarkEntitiesDirty`/`ClearDirtyEntities`/`RemoveDirtyEntities` 封装方法 |
| 6 | 消除 View 层 code-behind 中 `App.ServiceProvider` 直访 | N01 | 从 ~22 处减至 3 处（+2 弹窗回退）；新增 7 个 `App.*` V6 访问器 |
| 7 | 提取 GDH 静态可变状态到 `DataGridInteractionState` 注入服务 | R01, N01 | `GDH` 0 处静态可变字段（`_ctrlWasPressed`/`NavigationHandled`/`SuppressNextSelectionChanged`/`ColumnMetaCache` 全部提取）；新增 `Services/DataGridInteractionState.cs` + `App.DataGridState` |

---

## 补记 2026-07-17

### R09 脏提示细化 — HomePage 内联徽章

**变更前**：HomePage 有独立「Dirty Profiles」和「Standalone unsaved mods」区块，
分别列出脏 Profile 和脏 Mod，各有点击跳转命令。

**变更后**：脏状态标记内联到现有快捷入口：
- Recent Mods 条目右侧显示 `⚠ unsaved` 徽章（`HasUnsavedEdits` 属性）
- Profiles 条目右侧显示 `⚠ {n} dirty` 徽章（`HasUnsavedEdits` + `DirtyModCount`）
- 删除了 `DirtyProfileEntry`、`UnsavedModEntry` 记录类型及相关集合/命令
- `LoadDirtyStateAsync()` 直接标记现有 `RecentMods` / `Profiles` 条目

**影响文件**：
- `ViewModels/MainContent/HomePageViewModel.cs` — 重写脏状态加载逻辑
- `Views/UserControls/HomePage.axaml` — 移除独立区块，添加内联徽章绑定
- `Views/UserControls/HomePage.axaml.cs` — 移除已废弃的点击处理器

### Mod 数据库 XML 文件列表为空修复

**问题**：展开 Mod 项时 XML 文件列表为空。根因：`ModExpanded` 在目录不存在时
仍设置 `XmlFilePathsLoaded = true`，导致后续重试被跳过；且 `Receive(GameRootDirChangedMessage)`
为空桩，GameRootDir 变更后不重置加载状态。

**修复**：
- `ModExpanded`：目录不存在时不再标记 `XmlFilePathsLoaded = true`，允许重试
- `Receive(GameRootDirChangedMessage)`：重置所有 Mod 的 `XmlFilePathsLoaded = false`
  并清空 `XmlFilePaths`

**影响文件**：`ViewModels/ExplorerPane/ModDatabaseViewModel.cs`

## 补记 2026-07-18

### 审计状态全面修正

重新 grep 全代码库逐条验证后，发现多项先前标记为未完成/部分落地的规则
已在后续迭代中悄然完成，审计报告存在滞后。本补记修正以下条目：

| 条目 | 原状态 | 今状态 | 变更证据 |
|------|:--:|:--:|------|
| N04 死消息 | ❌ | ✅ | grep `GridRowHeightChangedMessage`/`FontSizeChangedMessage` — 0 处匹配 |
| Q10 死消息策略 | ❌ | ✅ | 3 条死消息全删，方案 A 已执行完毕 |
| Q7 AddRowDialog 工厂 | ⚠️ | ✅ | `Create(IConfigService, ...)` 已实现 (L134)；`ShowAsync`/`ShowSimpleAsync` 均接收注入的 `IConfigService` |
| R03 ReferenceResolver 静态 | ⚠️ | ✅ | `ReferenceResolver.cs` 0 处 `static` 修饰符；`_lookupCache`/`_cachedIndexService` 为实例字段；`BuildReverseIndexAsync`/`ResolveReverseRefs` 为实例方法 |
| R05 消息规范 | ⚠️ | ✅ | 死消息已删，残余问题消失 |

**新统计**：✅ 18 (69%) / ⚠️ 4 (15%) / ❌ 0 (0%) / 待验证 1 (4%) / 架构债 3 (12%)

**App.ServiceProvider 现状**（2026-07-18 重新扫描）：
- 全代码库 ~25 处引用，其中 ~22 处在 View 层 code-behind（UserControl 无法走 DI
  构造注入的 Avalonia 框架限制）。
- `ReferenceResolver`、`AddRowDialog`、`DependencyAnalysisService`、`Documents`、
  `EntityEditorDocument`、`ImagePreviewContent`、`KeyValueEditorViewModel` 等先前
  清单中的文件已不再有 `App.ServiceProvider` 直访。
- 弹窗类（`CreateModDialog`/`RenameImagePairDialog`）的参数化构造器保留
  `App.ServiceProvider` 回退是为 Avalonia XAML 加载器兼容——Q7=C 工厂方法已在
  所有运行时调用点使用。

**建议后续聚焦**：
1. T7（GDH 拆分/去静态化）— 当前最大架构债
2. M3/M4 功能里程碑

### T4/T5/T6 完成（2026-07-18 续）

第二轮验证发现 T4 和 T5 实际已完成，T6 本轮实施后亦完成：

| 条目 | 原状态 | 今状态 | 变更证据 |
|------|:--:|:--:|------|
| T4 GDH App.ServiceProvider | 待办 | ✅ | GDH 0 处 `App.ServiceProvider`；已改用 V6 `App.*` 访问器 |
| T5 DirtyEntities 封装 | 待办 | ✅ | 全量改用 `IWorkspaceSession.MarkEntityDirty`/`MarkEntitiesDirty`/`ClearDirtyEntities`/`RemoveDirtyEntities` |
| T6 App.ServiceProvider 消除 | 待办 | ✅ | 新增 7 个 `App.*` V6 访问器；View code-behind 从 ~22 处降至 3 处 |

**T6 影响文件**：

| 文件 | 关键改动 |
|------|---------|
| `App.axaml.cs` | 新增 7 个 V6 静态访问器属性 + 初始化 |
| `ModGameDataTabsView.axaml.cs` | 12 处 `App.ServiceProvider` → `App.*` 访问器 |
| `Pane.axaml.cs` | 2 处 `INavigationRouter` → `App.NavigationRouter` |
| `SearchResultsView.axaml.cs` | 1 处 `INavigationRouter` → `App.NavigationRouter` |
| `ReferenceInspectorView.axaml.cs` | 1 处 `IDbContextFactory<EditorDbContext>` → `App.EditorDbFactory` |
| `SearchableDataGrid.axaml.cs` | 1 处 `ILogger<SearchableDataGrid>` → `App.LoggerFactory.CreateLogger<>()` |
| `XmlDiffView.axaml.cs` | 1 处 `ILogger<XmlDiffView>` → `App.LoggerFactory.CreateLogger<>()` |

### T7 完成 — GDH 静态可变状态提取（2026-07-18 续）

T7 目标：消除 `GenericDataGridHelper` 中残余的静态可变状态（N01 违规），按职责提取到注入单例服务。

| 动作 | 详情 |
|------|------|
| 新建 `Services/DataGridInteractionState.cs` | 收容 `CtrlWasPressed`、`SuppressNextSelectionChanged`、`ColumnMetaCache` 三个活跃 flag |
| 删除 `NavigationHandled` | 死代码——声明后从未被读/写 |
| GDH `ColumnMetaCache` → `App.DataGridState.ColumnMetaCache` | 原 static Dictionary 改为属性委托 |
| GDH `_ctrlWasPressed` → `App.DataGridState.CtrlWasPressed` | 原 private static bool 改为属性委托（含 4 处内部引用替换） |
| GDH `SuppressNextSelectionChanged` → `App.DataGridState.SuppressNextSelectionChanged` | 原 auto-property 改为属性委托 |
| GDH `_router`/`_session` 保留 V6 属性模式 | 这两个本身就是 `App.NavigationRouter`/`App.WorkspaceSession` 的延迟属性，不持有状态 |
| `FieldDescriptions` 保留 | 启动时赋值一次，此后只读，非 N01 违规 |
| 外部消费者更新（3 处） | `SearchableDataGrid.axaml.cs` + `ModGameDataTabsView.Tab.cs` → `App.DataGridState.*` |

**T7 影响文件**：

| 文件 | 关键改动 |
|------|---------|
| `Services/DataGridInteractionState.cs` | **新建** — 注入单例，收容 3 个 DataGrid UI 交互 flag |
| `App.axaml.cs` | 新增 `DataGridState` V6 访问器 + DI 注册 + `OnFrameworkInitializationCompleted` 初始化 |
| `Helper/GenericDataGridHelper.cs` | 删除 4 个静态可变字段/属性；`_ctrlWasPressed` 4 处内部引用替换；`ColumnMetaCache`/`SuppressNextSelectionChanged`/`CtrlWasPressed` 改为委托属性 |
| `Views/UserControls/SearchableDataGrid.axaml.cs` | 8 处 `GenericDataGridHelper.SuppressNextSelectionChanged`/`ColumnMetaCache` → `App.DataGridState.*` |
| `Views/UserControls/ModGameDataTabsView.Tab.cs` | 2 处 `GenericDataGridHelper.SuppressNextSelectionChanged` → `App.DataGridState.*` |

**结果**：GDH 0 处静态可变字段，N01 完全合规 ✅

## 补记 2026-07-18 (续·T8)

### _copyBuffer 去静态化 (N01)

T7 消除了 GDH 中的 4 个静态可变字段，但遗漏了 `ModGameDataTabsView.axaml.cs`
code-behind 中的 `private static string? _copyBuffer`。该字段用作 DataGrid 单元格
复制/粘贴的内部缓冲区（避免系统剪贴板问题），是除 GDH 提取字段外最后一个
View code-behind 中的静态可变字段。

**修复**：`private static string? _copyBuffer` → `private string? _copyBuffer`

**影响文件**：`Views/UserControls/ModGameDataTabsView.axaml.cs`

---

## 补记 2026-07-24 — Bug 1 修复 + GDH 导航提取 + ServiceProvider 收紧

### Bug 1: KV 编辑器焦点跟随 Center Tab 切换 ✅

**问题**：用户点击 Dock DocumentDock 标签页时，左侧 KV 编辑器不跟随切换到对应实体（或 2s 延迟）。

**根因**：`EntityEditorView.axaml.cs` 的 `PointerPressed`/`AttachedToVisualTree`/`GotFocus`/`IsVisible`
事件在 Dock.Avalonia 标签页切换时不够可靠——点击标签 header 不触发内容区事件。

**修复**：
- `DocumentWorkspaceViewModel` 新增 `DockFactory.FocusedDockableChanged` 事件监听
- 当 FocusedDockable 的 Context 为 `EntityEditorDocument` 时，直接发送
  `ActiveEntityChangedMessage` + `_selection.SetCurrentEntity`
- 此事件在 Dock 框架层触发，无论 tab header 点击还是键盘切换都会可靠收到

**影响文件**：
- `ViewModels/MainContent/DocumentWorkspaceViewModel.cs` — 新增 `FocusDockableChanged` handler
- `Tests/CoreFlowTests.cs` — 修复 `App.WorkspaceSession` 未初始化导致的 4 个测试 NRE

### GDH 导航逻辑提取（架构债 #1 部分消除）

**变更前**：`GenericDataGridHelper` 的 `FindBestMatch`/`NavigateTo`/`NavigateToByEntityId`/
`PeekEntity`/`NavigateToReference`/`ResolveWithSecondary` 均为静态方法，通过 `App.*` V6
访问器获取 `IWorkspaceSession`/`IReferenceResolver`/`INavigationRouter`——违反 R04（Helper
层含导航逻辑）和 R07（Helper 层反向依赖 App.*）。

**变更后**：
- 新建 `Services/DataGridNavigationService`（注入式单例服务），实现 `IDataGridNavigationService` 接口
- 所有导航方法改为通过构造函数接收 `IWorkspaceSession` + `IReferenceResolver` + `INavigationRouter`
- GDH 原有静态方法保留签名不变，内部委托到 `App.DataGridNavigationService`
- `ResolveWithSecondary` 从 GDH 移除（现为 Service 的私有方法）

**影响文件**：
- `Services/DataGridNavigationService.cs` — **新建**（~175 行）
- `Helper/GenericDataGridHelper.cs` — 6 个方法简化为单行委托调用
- `App.axaml.cs` — 注册 `IDataGridNavigationService` + V6 访问器

**规则状态更新**：
- **R04**：⚠️ → ✅（导航逻辑已从 Helper 层移至 Services 层）
- **R07**：⚠️ → ✅（导航 Service 通过构造注入获取依赖，不依赖 App.*）

### App.ServiceProvider 进一步收紧（N01）

| 指标 | 2026-07-18 | 2026-07-24 |
|------|:--:|:--:|
| View code-behind 直访 | 3 处 | **2 处** |

**消除项**：`HomePage.axaml.cs:67` — `App.ServiceProvider.GetRequiredService<MainWindowViewModel>()`
→ 改用 `WeakReferenceMessenger.Default.Send(new NavigateToPageMessage(PageType.Settings))`

**保留项**（Avalonia XAML 加载器必需，Q7=C 工厂已覆盖运行时路径）：
- `CreateModDialog.axaml.cs:15` — 无参构造函数回退
- `RenameImagePairDialog.axaml.cs:15` — 无参构造函数回退

### 规则状态整体更新

| 规则 | 原状态 | 今状态 | 变更 |
|------|:--:|:--:|------|
| R04 | ⚠️ View 只组装控件 | ✅ | GDH 导航逻辑已提取为注入 Service |
| R07 | ⚠️ 单向分层 | ✅ | DataGridNavigationService 通过构造注入 |
| R10 | ⚠️ 索引过期角标待验证 | ✅ | IsExpired/MarkExpired/Refresh/TabTitle ⚠ 完整实现 |
| App.ServiceProvider 计数 | 3 | **2** | HomePage 消除 |

**新统计**：✅ 20 (77%) / ⚠️ 2 (8%) / ❌ 0 (0%) / 待验证 1 (4%) / 架构债 2 (8%)

### 架构债 #1 降级

GDH 的导航逻辑已提取为注入 Service。GDH 中剩余的 `ConfigureColumn`（~600 行 DataGrid 列模板 +
Ctrl+Click 事件处理）是 View 层 UI 构造代码，通过 `App.*` V6 访问器获取服务属于 Avalonia
框架约束下的合理模式。此条目从「违反 R04/R07」降级为「UI 层技术债——待 M6 拆分 ViewModel」。

### 推迟项：WAL/CommandHistory 迁移（架构债 #3 不变）

`ModGameDataTabsView` 的 `_commandHistory` + `RestoreCommandsFromLogAsync` +
`MarkTabsDirtyFromEditedCells` 深度耦合 View 内部状态（`Tabs`/`EditStore`/`MergeStore`/
`_dirtyTabs`），单独提取风险过高。需先为 ModGameDataTabsView 创建完整 ViewModel（M6）。

**影响方法**：`CopySelectedCells()` (L867-897)、`PasteCells()` (L900-958)——
两处均为实例方法，改为实例字段后语义不变。
